# VMO ハイクオリティ化 最終仕様書 v1.3

> **目標**: 誰が実装しても同じゲームになる詳細仕様

## 重要方針

> [!IMPORTANT]
> **既存対戦モードへの影響最小化**
> - 既存の `BattleEngine.js` のロジックは極力変更しない
> - アドベンチャー専用機能（下位技、レベル、経験値等）は別ファイルで管理
> - 対戦モードとアドベンチャーモードでデータを分離
> - 共通変更は「属性ID化」「UI改修」のみ
> - **ステータス4軸・技3つ（候補6つから選択）の現行仕様は維持する**（シンプルさが本作の売り）

---

## 1. 全体システム仕様

### 1.1 属性定義

| 内部ID | 日本語 | バッジ背景色 | 相性（2倍） | 相性（0.5倍） |
|--------|--------|-------------|-------------|---------------|
| `fire` | 炎 | 赤系 | grass | water, fire |
| `water` | 水 | 青系 | fire | grass, water |
| `grass` | 草 | 緑系 | water | fire, grass |
| `light` | 光 | 金/白系 | dark | light |
| `dark` | 闘 | 紫/黒系 | light | dark |
| `normal` | 無 | グレー系 | なし | なし |

※絵文字は使用しない。内部は英語ID、表示は漢字一文字バッジ

### 1.2 ステータス構成

**4軸固定**。物理/特殊の区別は設けない（シンプルさ優先）。

```javascript
{
  id: number,
  name: string,
  type: "fire" | "water" | "grass" | "light" | "dark" | "normal",
  hp: number,      // HP（5の倍数）
  atk: number,     // 攻撃（5の倍数）
  def: number,     // 防御（5の倍数）
  spd: number,     // 素早さ（5の倍数）
  moves: string[], // 候補技6つ
  img: string,
  desc: string
}
```

### 1.3 ステータスバランス

```javascript
const STAT_WEIGHTS = {
  hp: 1.0, atk: 1.3, def: 1.1, spd: 1.2
};
const TARGET_TOTAL = 500;
const TOLERANCE = 10;
// 既存モンスターの個性を維持しながら分割
```

### 1.4 技の数
- **候補**: 各モンスター6技
- **選択**: そこから**3技**を編成時に選ぶ（対戦・アドベンチャー共通）
- 「6つのうちどの3つを持たせるか」が編成の中心的な意思決定になるため、この比率は変更しない

---

## 2. 技システム仕様

### 2.1 汎用技

> [!NOTE]
> ステータスが4軸のため、物理/特殊の区別は**設けない**。
> 攻撃技のカテゴリは `physical`、変化技は `status` の2種類のみ。
> （旧仕様にあった「同威力の物理版/特殊版を両方用意する」案は廃止）

| 属性 | 単体技（威力） | 全体技（威力） |
|------|----------------|----------------|
| 炎 | フレイムバースト (110) | ヒートウェーブ (65) |
| 水 | アクアストリーム (110) | マッドウェーブ (65) |
| 草 | ソーンウィップ (110) | カッターウィンド (65) |
| 光 | ホーリーレイ (100) | フラッシュバン (65) |
| 闇 | ダークインパクト (100) | ダークミスト (65) |
| 無 | ラッシュ (90) | － |

### 2.2 バフ/デバフ技

#### バフ
| 技名 | 効果 | 対象 |
|------|------|------|
| パワーチャージ | buff_atk | ally |
| アイアンシェル | buff_def | ally |
| アクセルステップ | buff_spd | ally |

#### デバフ
| 技名 | 効果 | 対象 |
|------|------|------|
| インティミデイト | debuff_atk | all_enemies |
| アシッドボム | debuff_def | all_enemies |
| スパイダーネット | debuff_spd | all_enemies |

### 2.3 アドベンチャー専用技（下位互換）

属性ごとに1つ。カテゴリは全て `physical`。捕獲直後のヴァーモンが最初に知っている唯一の技（4.14参照）。

| 技名 | 属性 | 威力 |
|------|------|------|
| プチファイア | fire | 40 |
| アクアショット | water | 40 |
| リーフショット | grass | 40 |
| ミニレイ | light | 35 |
| シャドウタッチ | dark | 35 |
| タックル | normal | 30 |

---

## 3. バトルシステム仕様

### 3.1 ダメージ計算式

現行の `BattleEngine.js` の実装に準拠（4ステータス）。

```javascript
function calculateDamage(actor, target, move, rng) {
  const atk = actor.atk * getStatMultiplier(actor.buffs.atk);
  const def = target.def * getStatMultiplier(target.buffs.def);

  let damage = Math.floor((atk * move.power / def / 2));
  
  if (actor.level) {
    damage = Math.floor(damage * getLevelMultiplier(actor.level));
  }
  
  damage = Math.floor(damage * getTypeMultiplier(move.type, target.type));
  damage = Math.floor(damage * (0.9 + rng * 0.1));
  return Math.max(1, damage);
}
```

### 3.2 技演出仕様
- **実装**: CSS/SVGアニメーション
- **表示位置**: ターゲットのモンスター画像上
- **属性別**: 炎=赤炎、水=青波紋、草=緑葉、光=金閃光、闇=紫オーラ、無=灰衝撃波

---

## 4. アドベンチャーモード仕様

### 4.1 概要・ストーリー

- **舞台**: 地下研究施設「アーク」。ここで人工的に生み出された戦闘用モンスターは「**ヴァーモン**」と呼ばれる
- **主人公**: アークの研究者。施設の管理者に監禁され、人体実験に利用されかけていたところを、暴走したヴァーモンに監禁ポッドを破壊されて脱出のチャンスを得る
- **目標**: 地下深部（B30F）→地上（B1Fより上）への**脱出**
- **ラスボス**: ヴァーサス（B1F）。アーク産まれでありながら無限のエネルギー故に人間を超越し、施設の管理者として君臨する存在
- **エンディング**: 地上に脱出してクリア。だが実は地上も既にヴァーモンだらけになっており、それがクリア後のやり込みコンテンツ（後述4.13）に繋がる
- **実装状況**: B30～B1（ラスボス:ヴァーサス戦・エピローグまで）実装済み。クリア後のやり込みコンテンツ本体（4.13）は未実装

> [!NOTE]
> 自社ボードゲーム版（`vmo_bg`）にも同じ「アーク脱出」を題材にしたストーリーがあるが、階層構成やスケールは異なる。矛盾は気にせず「同じ脱出劇を異なる媒体・解像度で描いたもの」として扱う（世界線が違う扱い）。

### 4.2 初期選択（6体から2体）
| 属性 | 選択肢1 | 選択肢2 |
|------|---------|---------|
| 炎 | フレイミー | エンガール |
| 水 | ウォータル | スイネーク |
| 草 | ハッパンク | リーファム |

### 4.3 レベルシステム

> [!NOTE]
> MAX_LEVELは100を維持する（50への引き下げは検討したが、クリア後コンテンツ用の伸びしろが無くなるため見送り）。
> メインストーリー（1〜30階目、ラスボス:ヴァーサス）はLv50前後で足りるように調整し、Lv50〜100はクリア後のやり込み専用に残す。

```javascript
const MAX_LEVEL = 100;

function getLevelMultiplier(level) {
  // Lv1=0.3, Lv100=1.0
  // 0.007刻みだとLv100で0.993にしかならず、カンストしても種族値に届かない
  // （PvPの数値と一致しない）ため 0.7/99 で刻む
  return 0.3 + (Math.min(MAX_LEVEL, level) - 1) * (0.7 / (MAX_LEVEL - 1));
}

function getRequiredExp(level) {
  return Math.floor(10 * Math.pow(level, 1.8));
}
```

> [!IMPORTANT]
> **レベル補正はダメージだけでなくステータス（HP/ATK/DEF/SPD）にもかける。**
> ダメージのみに補正するとHPが据え置きになり、低レベルほど撃破ターン数が伸びて戦闘が間延びする
> （実測: Lv1で4.5ターン / Lv100で1.4ターン）。
> ステータス側にも同じ補正をかけると撃破ターン数がレベル非依存で一定になる（実測1.34ターン）。

#### 想定レベル帯（実測、プレイテストで調整）

実フロアデータで経験値シミュレーションした結果（入場時の自軍レベル）:

| フロア | 敵Lv | 入場時 自軍Lv | 差 |
|--------|------|---------------|-----|
| B30 | 2 | 1 | +1 |
| B29 | 4 | 4 | 0 |
| B28 | 6 | 5 | +1 |
| B27 | 8 | 7 | +1 |
| B26 | 10 | 9 | +1 |
| B25 | 12 | 11 | +1 |
| B24 | 14 | 14 | 0 |
| B23 | 16 | 16 | 0 |
| B22 | 18 | 18 | 0 |
| B21 | 19 | 20 | -1 |
| B20 | 20 | 22 | -2 |

B20クリア時 Lv23 / 累計61戦。1レベルあたり約2.5戦の一定ペース。
B1（30階目）到達時は約Lv70の想定。Lv70〜100はクリア後のやり込み用に残す。

### 4.4 経験値システム

> [!IMPORTANT]
> クリア済みフロアは何度でも再挑戦可能（欲しいヴァーモンを捕獲しに戻れる）。
> ただし周回による経験値稼ぎ（無双）を防ぐため、**自分が格上の場合は経験値をなだらかに減衰**させる。
> 極端な即0化はせず、格上差+10で概ね1/7程度まで下げつつ、それ以上格上でも僅かながら経験値は入り続ける（「いつかは上がる」を維持）。
> 逆に相手が格上の場合は今まで通り大きく増やし、**途中加入した低レベル個体を素早く追いつかせられる**ようにする。

```javascript
// 必要経験値が Lv^1.8 で伸びるのに対し取得側が線形(10+Lv*5)だと、
// Lv70到達に約1,467戦かかり本編164戦では到達不能だった。同じ次数で伸ばす。
function getBaseExp(enemyLevel) {
  return Math.max(1, Math.floor(4 * Math.pow(enemyLevel, 1.8)));
}

function getExpMultiplier(myLevel, enemyLevel) {
  const diff = enemyLevel - myLevel; // 正の値＝相手が格上（美味しい）
  if (diff >= 10) return 3.0;
  if (diff >= 5)  return 2.0;
  if (diff >= 1)  return 1.5;
  if (diff === 0) return 1.0;
  // diff < 0: 自分が格上。指数関数的になだらかに減衰する（diff=-10で概ね1/7、下限0.02）
  const decayBase = Math.pow(1 / 7, 1 / 10);
  return Math.max(0.02, Math.pow(decayBase, -diff));
}

function getExp(baseExp, myLevel, enemyLevel) {
  return Math.max(1, Math.floor(baseExp * getExpMultiplier(myLevel, enemyLevel)));
}
// 全所持モンスターが同量取得（レベル差補正は各自適用）
```

### 4.5 仲間加入

基礎捕獲率に加えて、**相手のレベルが高いほど捕獲しにくくなる**補正を導入する（終盤の高レベル個体を無条件で捕まえられないようにするため）。

```javascript
const CAPTURE_RATES = {
  normal: 0.30, // 通常の野生ヴァーモン
  elite: 0.15,  // 強化個体（エリート戦闘）
  boss: 0.10    // フロアボス
};

// 同調デバイス（捕獲率アイテム）。Mk-Vのみ確定捕獲でレベル補正を無視する。
// レベル補正後の捕獲率に固定加算する方式。倍率方式だと、レベル補正で
// 捕獲率が下がった相手ほどデバイスの効果まで一緒に目減りし、
// 高レベル相手ほど「高いデバイスを使った実感」が薄れる問題があったため
const CAPTURE_DEVICES = {
  "同調デバイス Mk-I":   0.10, // +10%
  "同調デバイス Mk-II":  0.20, // +20%
  "同調デバイス Mk-III": 0.35, // +35%
  "同調デバイス Mk-IV":  0.55, // +55%
  "同調デバイス Mk-V":   "guaranteed" // 100%固定
};

function getCaptureRate(tier, targetLevel, deviceName = null) {
  const device = deviceName ? CAPTURE_DEVICES[deviceName] : 0;
  if (device === "guaranteed") return 1.0;

  const base = CAPTURE_RATES[tier];
  const levelPenalty = Math.max(0.3, 1 - (targetLevel - 1) * 0.008); // Lv100で最低0.3倍まで低下
  return Math.min(1, base * levelPenalty + device);
}
// 同一モンスターは1体のみ
```

#### 捕獲フロー

自動判定にすると「デバイスを使う瞬間」をプレイヤーが選べず、所持している最上位デバイスを
毎戦闘で消費してしまう。かつ成功率が3割前後のため、失敗時に何も表示されないと
「捕獲システムが無い」ように見える。そのため**選択式**にする。

- **所持済みの種族**を倒した場合は捕獲画面を出さない（周回のテンポを維持）
- **未所持の種族**を倒した場合のみ捕獲画面を表示し、「素手で試す／各デバイスを使う／何もしない」を選ばせる
- 各選択肢に**成功率を明示**する（デバイスのグレード選択が意味のある判断になる）
- 選択後は必ず「CAPTURED!／ESCAPED...」の結果画面を表示する

### 4.6 ショップ

> [!IMPORTANT]
> アイテムはバトル中は使用不可。拠点・休憩でのみ使用できる。

```javascript
// 通貨: 円（乱数幅あり）
function getDropMoney(enemyLevel) {
  const base = 110 + enemyLevel * 18;
  const variance = Math.floor(base * 0.2); // ±20%
  return base + Math.floor(Math.random() * variance * 2) - variance;
}

const SHOP_ITEMS = [
  { name: "薬草",         price: 50,   effect: "HP30%回復" },       // 1.67円/%
  { name: "回復薬",       price: 80,   effect: "HP50%回復" },       // 1.60円/%
  { name: "アドレナリン", price: 120,  effect: "HP100%回復" },      // 1.20円/%（上位ほど円/%が下がる＝薬草だけで済ませない設計）
  { name: "万能薬",       price: 150,  effect: "状態異常を全回復" },
  { name: "蘇生器",       price: 300,  effect: "戦闘不能から50%HPで復活" },
  { name: "回復スプレー", price: 260,  effect: "パーティ全員HP50%回復（対象選択なしで即使用）" },
  { name: "解毒スプレー", price: 200,  effect: "パーティ全員の状態異常を全回復（対象選択なしで即使用）" },
  { name: "同調デバイス Mk-I",   price: 150,   effect: "捕獲率+10%" },
  { name: "同調デバイス Mk-II",  price: 350,   effect: "捕獲率+20%" },
  { name: "同調デバイス Mk-III", price: 1000,  effect: "捕獲率+35%" },
  { name: "同調デバイス Mk-IV",  price: 2000,  effect: "捕獲率+55%" },
  { name: "同調デバイス Mk-V",   price: 5000,  effect: "捕獲率100%（確定）" }
];
// 価格は暫定値。プレイテストで調整する
```

### 4.7 セーブシステム
- **方式**: Firebase Authentication（Googleログイン）
- **保存先**: Firestore
- **保存内容**: 所持モンスター、レベル、覚えた技、所持金、攻略進捗

### 4.8 探索フェーズ（ノード選択システム）

「進む」を4択にし、選択前にそれぞれの気配（危険度ヒント）を表示する。**マップは持たず、内部的には各選択肢の抽選テーブルを切り替えるだけ**（ノードが見えない版のローグライク）。

```
┌─────────────────────────────────────┐
│ ダンジョン進行画面                   │
├─────────────────────────────────────┤
│  現在地: B28 炎の実験エリア          │
│  残り休憩回数: 2/3                  │
│                                     │
│  ⚔️ 安全に進む      （静かだ）       │
│  🔍 索敵する        （強い気配）     │
│  🎒 アイテムを探す   （何かの匂い）   │
│  🎲 とにかく進む     （？）          │
│                                     │
│         [ 休憩する ]                │
└─────────────────────────────────────┘
```

**ヒントの仕組み**: 画面表示時に4択それぞれの結果を裏で先に抽選しておき、その結果に応じたアイコン/一言のみを見せる。プレイヤーは中身を完全には知らないが傾向は読める。

#### 選択肢ごとの抽選テーブル

| 選択肢 | 通常戦闘 | エリート戦闘 | アイテム | 罠 |
|--------|:-------:|:-----------:|:-------:|:--:|
| ⚔️ 安全に進む | 100% | 0% | 0% | 0% |
| 🔍 索敵する | 60% | 35% | 0% | 5% |
| 🎒 アイテムを探す | 45% | 0% | 35% | 20% |
| 🎲 とにかく進む | 30% | 20% | 25% | 25% |

- **安全に進む**: 常に通常の野生ヴァーモン戦のみ。低リスク・低リターン
- **索敵する**: エリート（強化個体・研究員）に当たりやすい代わりに、わずかに罠のリスクもある（アイテムはなし）。育成・捕獲狙いのルート
- **アイテムを探す**: アイテムに当たりやすいが、罠のリスクも背負う。エリートには当たらない
- **とにかく進む**: 全パターンが起こりうる広く浅いテーブル。索敵・アイテム双方の旨味を同時に狙えるギャンブラー向け

上記の%は初期値であり、プレイテストで調整する前提の暫定値。

- **休憩**: HP30%回復のみ（技・パーティ入れ替えは不可。4.17拠点を参照）
- **拠点に戻ると全回復**（撤退・全滅・フロアクリアいずれも）。回復薬を買わないと再挑戦できないのは苦行なため
- **休憩回数**: 階層ごとに決まった回数まで（例: B30=2回、B25=3回）
- **ボス**: 各階最後に固定ボス（ノード選択を挟まず必ず遭遇）

### 4.9 罠システム

「アイテムを探す」「とにかく進む」で発生しうる非戦闘イベント。戦闘は発生しない。

| 罠 | 効果 | 抽選比率 |
|----|------|:-------:|
| ダメージ罠 | 全体に10ダメージ | 30% |
| 毒罠 | 全体に毒状態を付与（次の戦闘中、継続ダメージ） | 20% |
| 資金ロス罠 | 所持金の15%を失う | 20% |
| 休憩消費罠 | このフロアの残り休憩回数-1 | 15% |
| 弱体化罠 | 次の戦闘、全体のいずれか1ステータスがデバフ状態で開始 | 15% |

### 4.10 敵編成・2体エンカウント率

野生ヴァーモンは1体または2体（ダブルバトル、既存の2vs2エンジンをそのまま使用）で出現する。**フロアごとにリセットし、そのフロア内で奥へ進むほど2体の確率が上がる**。さらに深いフロア（B30側）ほど基礎確率が低く、浅いフロア（B1側）に近づくほど開始時点から2体が出やすくなる。**上限は90%**（終盤でも1体エンカウントの可能性を必ず残す）。

```javascript
// フロアごとに { baseRate: 入場時点の2体率, rampPerStep: 1ステップ進むごとの上昇率 } を持つ
const DOUBLE_ENCOUNTER_TABLE = {
  B30: { baseRate: 0.00, rampPerStep: 0.03 },
  B25: { baseRate: 0.10, rampPerStep: 0.05 },
  B21: { baseRate: 0.25, rampPerStep: 0.08 }, // 終盤は最初から2体が当たり前に
};
// 実際は階層ごとに個別設定。上記は代表値、線形補間 or 個別指定は実装時に調整
```

| パターン | 説明 | 補正 |
|----------|------|------|
| 通常戦闘 | 野生ヴァーモン1〜2体（上記テーブルに従う） | - |
| エリート戦闘 | 強化された野生ヴァーモン／雑魚研究員／エリート研究員のいずれかを抽選（下記4.16参照） | HP/ステ強化、報酬UP |
| ボス戦 | 各フロア最後の固定ボス1体 | HP/ステ×1.5倍 |

- **改造研究員**: 個性は設けないが、**雑魚研究員とエリート研究員の2段階**を用意する。雑魚研究員は野生の雑魚戦と同等の弱い編成、エリート研究員はアーキタイプチーム（4.16）を使う本気の編成。深い階層に進むほどエリート研究員の出現比率が上がる
- **序盤3フロア（B30-B28）は炎水草の混合**にする。単一属性テーマだと初期選択の属性次第で常時1.5倍被弾になり、初回ボスで詰む。属性テーマはB27（光）から始める
- **ボスに初期選択6体を使わない**。自分と同じヴァーモンと戦う違和感を避ける
- **B30はチュートリアル**。ボスはシカバラス（無属性＝弱点スパイクなし）、ステータス補正なし(1.0)、技は手動指定（`floor.bossMoves`）。自動編成だと単体攻撃がラッシュ(P90)しかなく、初期技(P30-40)のLv1パーティに対し火力差が2倍以上つくため

#### 敵の技威力上限（`getFloorPowerCap`）

プレイヤーは最初の上位攻撃技をLv7まで持てず、それまで下位技(P30-40)で戦う。
敵だけが2階目からP110を振り回すと威力差2.75倍になり、ステータス補正をいくら下げても
クリア不能になる。そのため**敵の使える技の威力にもフロア連動の上限**を設ける。

| フロア位置 | 威力上限 |
|-----------|---------|
| 1階目 | 40 |
| 2階目 | 65 |
| 3階目 | 70 |
| 4階目 | 80 |
| 5階目 | 90 |
| 6階目 | 100 |
| 7階目以降 | 制限なし |

野生・エリート・ボスすべてに適用する。

#### 難易度の実体はパーティ数

シミュレーションの結果、難易度を最も左右するのは**手持ちの数**だった
（手持ち2体だと全フロア攻略不能、4体だとほぼ全フロア攻略可能）。
序盤は捕獲が進んでおらず2体しかいないため、フロアごとに敵を弱体化して調整する。

| フロア | wildStatMult | bossTier | 想定手持ち | クリア率（実測） |
|--------|-------------|----------|-----------|-----------------|
| B30 | 0.7 | normal(1.0) | 2 | 100% |
| B29 | 0.8 | elite(1.2) | 3 | 100% |
| B28 | 0.8 | normal(1.0) | 3 | 96% |
| B27 | 1.0 | elite(1.2) | 4 | 76% |
| B26 | 1.0 | elite(1.2) | 4 | 85% |
| B25〜B20 | 0.9 | elite(1.2) | 4 | 93〜100% |
| B19〜B2 | 0.8〜0.9 | elite(1.2) | 5〜7 | 69〜100% |
| B1（ヴァーサス） | — | 専用ステータス上書き | 0 | Lv73未満で0%、Lv73到達で辛勝、Lv80以降は余裕 |

- ボスレベルは既定で `floor.level + 1`（+2 だと序盤のボス戦だけ突出して難しくなる）
- `boss` ティア(1.5倍)はB19〜B2のゾーン最終フロアで試したが強すぎたため不採用。`elite`(1.2倍)+`wildStatMult`調整の組み合わせに統一した
- 育成をサボった場合（Lv2・2体のまま）はB29以降ほぼ0%で、「整えないと厳しい」性質は保たれている。B19〜B1でも手持ち2体では想定レベル到達時点でもほぼ勝てず、同じ性質を維持
- **難易度方針**: ボス単体を歪に強くするのではなく、2体エンカウント率とエリート出現率を主なレバーにする。手持ちの層が薄いパーティほど同時多体戦で崩れやすく、「ちゃんと仲間を集めてレベルを整えていればイーブン、サボると厳しい」という難易度カーブを狙う
- **ボスのバリエーション（終盤）**: 単独ボスばかりだと「一体を集中攻撃すれば余裕で勝てる」という単調さが出るため、B2〜B7ゾーンの一部を差し替える
  - `bossDuo: true`（B5・B2）: 同種ボスを2体に。個体ごとの補正は`normal`(1.0倍)まで下げ、単体を歪に強くしない方針を保ったまま、同時2体を相手取る戦術的な難しさで強さを出す（合計HP/火力は旧`elite`単体比で約1.67倍、各個体のHPは旧比83〜84%＝1体落とせば火力が半減する設計）
  - `bossKind: 'researcher'`（B3）: フロアボスの代わりにエリート研究員編成（RESEARCHER_TEAMS、4体アーキタイプ）と戦う。既存のエリート研究員遭遇と同じ強さだが、フロアボスとして確定発生する点が異なる

### 4.11 階層構成（B30-B20）

| 階層 | 名称 | 敵Lv | 戦闘数 | 休憩 | 野生種 | ボス |
|------|------|------|--------|------|--------|------|
| B30 | 第三実験区画 | 2 | 3 | 2 | ヒノエナガ(炎)、コペゾー(水)、ハナーネ(草) | シカバラス(無) |
| B29 | 飼育プール | 4 | 3 | 2 | ジェケイダ(炎)、スイネーク(水)、フラビット(草) | イグニルフ(炎) |
| B28 | 培養温室 | 6 | 4 | 2 | バラビィ(炎)、ビョウゲツ(水)、モモ(草) | ペパザール(水) |
| B27 | 光の研究室 | 8 | 4 | 2 | ツキネ、リュミエット、オヌ・リン | ライトラ(光) |
| B26 | 闇の実験場 | 10 | 4 | 2 | ハリースト、モスパーク、ウィデビット | エルダーク(闇) |
| B25 | 混合エリア(炎+水) | 12 | 5 | 3 | エンガール、ビョウゲツ、ヒノエナガ、コペゾー | レイコーン(水) |
| B24 | 混合エリア(草+光) | 14 | 5 | 3 | シバフールー、オヌ・リン、ハナーネ、ツキネ | アカリード(光) |
| B23 | 廃棄エリア | 16 | 5 | 3 | ウィデビット、ハリースト、モスパーク、スイネーク | ザルディヴァ(闇) |
| B22 | 高速テストエリア | 18 | 5 | 3 | ジェケイダ、リュミエット、ツキネ、ハリースト | フィンレーツ(闇) |
| B21 | 重装テストエリア | 19 | 6 | 3 | ロクザール、ジェリスタル、シバフールー、フラビット | マグマス(炎) |
| B20 | 中間管理エリア | 20 | 6 | 4 | シャコマル、デヴィートル、オベアー、レイコーン | ミレメント(無) |

- B30〜B28は炎水草の混合。属性テーマはB27（光）から始める（4.10参照）
- B22（高速テストエリア）は明確な属性テーマがないため、既出モンスターの中から素早さの高い個体だけを集めて構成する
- B20は初回リリース最終フロアのため、過去フロアの元ボス格（レイコーン等）が並みの敵として現れる。「今の実力なら格下」という成長の実感を狙う

### 4.11b 階層構成（B19-B1）

ゾーンA〜Dはボスを「弱→強」の順で各フロアに割り当て、ゾーン最終フロアは1段階強い調整（B16/B11/B6/B2）。新規モンスターは追加せず、既出31種（初期選択3種含む）の使い回し＋一部再登場（後述）でB1（ラスボス:ヴァーサス）まで完結させている。

| ゾーン | 範囲 | 名称 |
|--------|------|------|
| A | B19〜B16 | 警備強化区画（脱走が発覚し警備が厳重に） |
| B | B15〜B11 | 崩壊隔離区画 |
| C | B10〜B6 | コアシステム区画（管理中枢に接近） |
| D | B5〜B2 | 最終防衛ライン |
| — | B1 | 地上ゲート（ラスボスのみ、雑魚戦なし） |

| 階層 | 名称 | 敵Lv | 戦闘数 | 休憩 | 野生種 | ボス |
|------|------|------|--------|------|--------|------|
| B19 | 監視回廊 | 23 | 5 | 3 | ヒノエナガ、コペゾー、ハナーネ、ジェケイダ | モモ |
| B18 | 非常呼集区画 | 25 | 5 | 3 | スイネーク、フラビット、ビョウゲツ、ツキネ | ジェリスタル |
| B17 | 検問ゲート | 27 | 5 | 3 | リュミエット、オヌ・リン、ハリースト、モスパーク | アルブラン |
| B16 | 武装保管庫 | 29 | 5 | 3 | ウィデビット、エンガール、シバフールー、ロクザール | バラビィ |
| B15 | 亀裂拡大区画 | 32 | 6 | 4 | シャコマル、デヴィートル、レイコーン、モモ(再) | イグニルフ(再) |
| B14 | 隔離病棟跡 | 35 | 6 | 4 | ペパザール、ライトラ、アカリード、ジェリスタル(再) | ツルギ |
| B13 | 陥没通路 | 38 | 6 | 4 | フィンレーツ、ミレメント、フレイミー、アルブラン(再) | ベルフレム |
| B12 | 汚染浄化区画 | 41 | 6 | 4 | ハッパンク、リーファム、ヒノエナガ、バラビィ(再) | シザール |
| B11 | 隔壁制御室 | 44 | 6 | 4 | ハナーネ、ジェケイダ、スイネーク、フラビット | アッピオン |
| B10 | 冷却プラント | 47 | 6 | 4 | ビョウゲツ、ツキネ、リュミエット、イグニルフ(再) | ユッキング |
| B9 | 電力中枢 | 50 | 6 | 4 | ハリースト、モスパーク、ウィデビット、ツルギ(再) | ストロビー |
| B8 | データバンク | 53 | 6 | 4 | シバフールー、ロクザール、シャコマル、ベルフレム(再) | ボルクマ |
| B7 | サーバー回廊 | 56 | 6 | 4 | レイコーン、シカバラス、ペパザール、シザール(再) | エルシェント |
| B6 | 中央制御室 | 59 | 6 | 4 | アカリード、ザルディヴァ、フィンレーツ、アッピオン(再) | グラシオン |
| B5 | 第一防衛ライン | 62 | 7 | 5 | フレイミー、ウォータル、ハッパンク、リーファム | リフェロス |
| B4 | 対ヴァーモン兵装区画 | 65 | 7 | 5 | ヒノエナガ、コペゾー、ハナーネ、ユッキング(再) | オベアー |
| B3 | 最終検問 | 68 | 7 | 5 | スイネーク、フラビット、ビョウゲツ、ストロビー(再) | エルダーク(再) |
| B2 | 地上直結エレベーター | 71 | 7 | 5 | リュミエット、オヌ・リン、ハリースト、ボルクマ(再) | マグマス(再) |
| B1 | 地上ゲート | 71 | 0 | 0 | （雑魚戦なし） | ヴァーサス |

- 全フロア `wildStatMult` を0.88〜0.9（一部0.8）で補正。B19以降は敵の技威力上限(`getFloorPowerCap`)が撤廃済みで純粋にステータス勝負になるため、B25〜B20の路線を踏襲して野生側を少し弱める調整を継続している
- ゾーン最終フロア（B16/B11/B6/B2）はボス格を1段引き締める意図で導入したが、`bossTier: 'boss'`(1.5倍)は強すぎたため`'elite'`(1.2倍)+ステータス補正の組み合わせに落ち着いた
- D帯にエルダーク・マグマスを再来させるのは、「B26/B21で苦戦した相手が終盤でもまだ通用する強さ」という格を保ちつつ、初見ボスではなく既知の強敵として最終防衛ラインに配置する狙い。同様にイグニルフ・ジェリスタル・アルブラン・バラビィ・ツルギ・シザール・ボルクマ・ユッキング・ストロビー・アッピオンも一度ボスとして登場した後、後続ゾーンで「今の実力なら格下」の野生として再登場する（(再)表記）
- **B1（ヴァーサス戦）**: `monsters.js`のid999データはHP9999の管理者イースターエッグ用（対戦シミュレーターのGENESIS隠しボス）のため、アドベンチャーではそのまま使わず`floor.finalBossStats`で実効ステータスを直接上書きする（`{hp:280, atk:80, def:80, spd:110}`、Lv80、技は`カースドノヴァ`/`ラッシュ`/`プロテクション`固定）。実測: 自Lv73未満の手持ち4体編成では敗北、Lv73到達直後は僅かに余裕を残して辛勝、Lv80以降は余裕を持って勝てる曲線
- 全フロアに個別の物語を書くのは非現実的なため、ボス階・エリート研究員が出る階だけ固有の描写を作り、それ以外はゾーン共通のフレーバーテキストを使い回す方針を維持

### 4.12 探索テキスト演出

「進む」を押した瞬間に一言フレーバーを挟み、歩いて遭遇した感を出す（マップや座標計算は不要、固定テキスト配列からの抽選/進行度切り替えのみ）。

- **遭遇前の一言**: ゾーンごとに数パターン用意し、結果（通常/エリート/アイテム/罠）に応じたトーンで表示（例: 「奥から低い唸り声が聞こえる…」）
- **フロア内の進行描写**: 同じフロアでも奥に進むほど描写を変える（入口「静かな通路」→中盤「焦げた壁」→ボス直前「熱気がこもる大部屋」）。固定テキスト配列を進行度で切り替えるだけで実装可能
- **休憩時の一言**: 地上に近づくフロアほど、明るさ・空気の変化を匂わせる一言を添える

### 4.13 クリア後コンテンツ（やり込み要素）

地上に脱出してエンディングを迎えた後、**実は地上も既にヴァーモンだらけになっている**ことが判明し、そのままやり込みコンテンツに接続する。詳細は続編フェーズで設計するが、以下を想定：

- 地上エリアの探索（本編と同じノード選択システムを流用）
- 強力な個体・専用ボスとの対峙
- 図鑑コンプリートの最終ピース
- 敵の想定レベルはLv50前提（4.3参照）で、Lv50〜100の育成に意味を持たせる

### 4.14 技の習得システム（プレイヤー側）

捕獲直後は下位技（2.3）を1つだけ知っている状態からスタートする。以降、レベルアップでプレイヤーが好きな技を選んで習得していく（習得順を配列順に固定しない＝どのモンスターも「攻撃技だけ先に並んでいる」保証がないため、選択制にすることで解決する）。

```javascript
const MOVE_LEARN_CHECKPOINTS = [3, 7, 11, 16, 22, 29]; // 候補6技すべてに対応。Lv29で全習得
```

**初期技は2つ**: 属性下位技（威力35-40）＋ 無属性のタックル（威力30）。
無属性は半減されないため「弱点を狙うか安定を取るか」の選択がLv1から生まれる。
無属性モンスターは属性下位技がタックルと重複するため、例外として1つだけ持つ。

**最初の習得（Lv3）は必ず補助技**。初期技が下位攻撃技2つなので、
いきなり上位攻撃技を配ると下位技が即座に死蔵される。

- 各チェックポイント到達時、まだ知らない候補技（monsters.jsの6技）からプレイヤーが1つ選んで習得
- 装備（実際にバトルで使う3技）の入れ替えは**拠点でのみ**可能（4.17）。ダンジョン内の休憩ではHP回復のみ
- **途中加入（例: Lv20で捕獲）の場合**: 通過済みのチェックポイント数ぶんを自動で習得済みにする（順序は4.16の攻撃技優先ロジックを流用）。プレイヤーの選択機会は失われるが、装備は拠点で自由に選べるため実害はない

```javascript
function getKnownMovesOnCapture(monster, level) {
  const passedCheckpoints = MOVE_LEARN_CHECKPOINTS.filter(lv => level >= lv).length;
  const autoOrder = [...monster.moves].sort((a, b) =>
    (dbMoves[a].category === 'status' ? 1 : 0) - (dbMoves[b].category === 'status' ? 1 : 0)
  ); // 攻撃技を先に並べる
  return [STARTER_MOVES[monster.type], ...autoOrder.slice(0, passedCheckpoints)];
}
```

### 4.15 野生ヴァーモンの技構成（フロア位置依存）

野生の雑魚戦は**個体のレベルではなくフロアの深さ（潜った階数）**で技構成が決まる。浅い階の実験体ほど未熟、という世界観の演出も兼ねる。

```javascript
function getWildMoveSet(monster, floorPosition) {
  const starter = STARTER_MOVES[monster.type];
  if (floorPosition <= 1) return [starter]; // 1階目: 下位技のみ

  const realCount = Math.min(6, Math.round((floorPosition - 1) / 9 * 6)); // 1→10階目で0→6に線形解放
  const realMoves = [...monster.moves].sort(() => Math.random() - 0.5).slice(0, Math.min(3, realCount || 1));

  if (floorPosition < 15) return [starter, ...realMoves].slice(0, 3); // 下位技と実技が混在
  return realMoves.length ? realMoves : [starter]; // 15階目以降: 下位技は使われなくなる
}
```

- 1階目: 下位技のみ（最弱）
- 10階目: 実技が全解放、下位技と混在してランダム抽選
- 15階目以降: 下位技を使わなくなり、実技のみで構成

### 4.16 エリート/ボス/研究員の技構成（アーキタイプチーム）

エリート・ボス・エリート研究員は「威力の高い技を並べる」だけでなく、**シングルバトルに既にあるディストーションパーティのような、明確なコンセプトを持つ編成**にする。個別に手作業で全モンスター分組むのは非現実的なため、少数のアーキタイプテンプレートを手動で用意し、そこから抽選する。

```javascript
// エリート個体・フロアボスの技: 威力順の攻撃技2つ+補助技1つを機械的に自動編成
// 下位技は枠を埋めるだけの弱技なので除外する（「本気の構成」にするため）
function getEliteMoves(monster, level) {
  const starter = STARTER_MOVES[monster.type];
  const known = getKnownMovesOnCapture(monster, level).filter(m => m !== starter);
  const attack = known.filter(m => dbMoves[m]?.category !== 'status')
                       .sort((a, b) => dbMoves[b].power - dbMoves[a].power);
  const support = known.filter(m => dbMoves[m]?.category === 'status');
  let equipped = attack.slice(0, 2);
  equipped.push(support[0] || attack[2]);
  return equipped.filter(Boolean);
}

// 雑魚研究員: 野生雑魚と同等の弱い編成（1〜2体、getWildMoveSet相当）
// エリート研究員: 手動作成したアーキタイプテンプレートから抽選（4体、既存の対戦用データをそのまま流用）
const RESEARCHER_TEAMS = [
  { name: "ディストーション速攻", members: [...] },
  { name: "全体バフ特化", members: [...] },
  { name: "デバフ+削り", members: [...] },
  { name: "プロテクション粘り", members: [...] }
];
```

- 「エリート」を引いた際、強化された野生個体／雑魚研究員／エリート研究員のどれになるかをフロアの深さで振り分ける（`getEliteEncounterKind`）。B27相当以前（position≤4）は強化個体のみ、以降じわじわ雑魚研究員→エリート研究員の比率が上がり、B11以降（position≥20）はエリート研究員が60%を占める
- 「安全に進む」もフロア位置に応じてわずかにエリート（強化個体/研究員）が混ざるようにした（`getSafeEliteChance`）。B23相当まで(position≤8)は0%、以降じわじわ上がり5%で頭打ち
- **エリート研究員(4体)のステータス補正**: 実測の結果、4体全員に`elite`(1.2倍)を掛けると2v2エンジン上で数の暴力になりすぎて自Lv73想定パーティでも0%だったため、個体ステータスは`normal`(1.0倍・floor.levelそのまま)に据え置き、編成の噛み合わせ自体を強さにする方針に変更。この設定で自然到達レベル帯のクリア率は50〜75%（育成不足だとほぼ0%）
- アーキタイプテンプレートは既存のPvP用データ（app.jsのELITE_TEAMS/MASTER_TEAMS）と同じ思想で作成。新規モンスターやモーション追加は不要

### 4.17 拠点（ベース）

`img/assets/base_bg.webp` を背景にしたソシャゲ風ホーム画面。
上部にパーティのサムネイル、**中央に大きな「出撃」ボタン**、下部にサブメニュー（パーティ/技/ショップ/道具）。
出撃を押すと階層選択画面に遷移し、各フロアの**適性レベル・クリア状況・戦闘数/休憩数**を表示する。
**クリア済みフロアは出現するヴァーモンとボスを一覧表示**し、未捕獲の個体には★を付ける（捕獲目的の再訪を支援）。

ダンジョンに潜る前の拠点。以下はここでのみ行える：

- **パーティ編成**: 手持ち4体の入れ替え（ボックスとの入れ替え含む）。並び替え・ボックスとの入れ替えは
  ドラッグ（タッチはスワイプ）、またはボックス側をタップ選択→入れ替え先の手持ちをタップの2通りで行える
- **技の装備変更**: そのモンスターが習得済みの技から3つを選んで装備
- **ショップ利用**: 4.6のアイテム購入
- **アイテム使用**: バトル中は使えないため、回復・状態異常回復・蘇生はここか休憩でのみ

---

## 5. UI仕様

### 5.1 技コマンド（3技縦並び、情報量維持）

```
┌────────────────────────────────────────┐
│ モンスター名                    [BACK] │
├────────────────────────────────────────┤
│ ┌──────────────────────────────┐ ┌──┐ │
│ │ 技1名        [炎] P:110      │ │⇄│ │
│ │ 説明文テキスト...            │ │交│ │
│ ├──────────────────────────────┤ │代│ │
│ │ 技2名        [水] P:110      │ │  │ │
│ │ 説明文テキスト...            │ │  │ │
│ ├──────────────────────────────┤ │  │ │
│ │ 技3名        [草] P:65       │ │  │ │
│ │ 説明文テキスト...            │ └──┘ │
│ └──────────────────────────────┘      │
└────────────────────────────────────────┘
```
- 縦並び1列、現在と同じ表示形式
- 技名・属性バッジ・威力・説明文を常時表示
- 3技なので1つあたりの表示領域に余裕がある。情報量を削らない

### 5.2 背景画像

| 画面 | 背景 |
|------|------|
| タイトル | menu_bg.png |
| 対戦バトル | **新規作成**（電子フィールド） |
| 拠点 | base_bg.webp |

### 5.3 メニュー構成
```
[ADVENTURE MODE] ← 最上部
[SINGLE BATTLE]
[ONLINE BATTLE]
[TEAM EDIT] [MONSTER DATA]
```

---

## 6. ファイル構成

### 新規作成
| ファイル | 内容 |
|----------|------|
| AdventureMode.js | 拠点+ダンジョン選択 |
| AdventureBattle.js | レベル補正バトル |
| AdventureData.js | 全データ |
| SkillEffects.js | 技演出 |
| img/battle_bg.webp | 対戦背景 |

### 変更ファイル
| ファイル | 変更 |
|----------|------|
| monsters.js | type文字列化（ステータスは4軸のまま） |
| moves.js | type文字列化 |
| BattleEngine.js | 背景差し替え（技UI・計算式は現行維持） |
| Shared.js | バッジ対応 |
| app.js | メニュー変更、Firebase Auth統合 |

---

## 7. 実装順序

> [!NOTE]
> 旧Phase「技4つ対応」「6ステータス化」は廃止。ステータス4軸・技3つは現行のまま維持する方針に決定。

以前のPhase分けは粒度が粗すぎたため、実際の作業単位に合わせて分割する。**各Phaseの終わりに必ず対戦モード（既存PvP）が壊れていないことを手動確認してから次に進む**（`BattleEngine.js`/`Shared.js`/`gameLogic.js`/`monsters.js`/`moves.js`は対戦モードと共有のため、ここを触るPhaseは特に注意）。

1. **Phase 0**: 属性文字列ID化（絵文字→英語ID、バッジ表示）
2. **Phase 1**: 対戦背景追加（`img/battle_bg.webp`、UI差し替えのみ）
3. **Phase 2**: データ基盤（`AdventureData.js`新規作成）
   - 下位技6つを`moves.js`に追加
   - `STARTER_MOVES` / `MOVE_LEARN_CHECKPOINTS` / `DOUBLE_ENCOUNTER_TABLE` / `CAPTURE_RATES`（`AdventureCapture.js`は実装済み）
   - フロアデータ（B30〜B1、名称・戦闘数・休憩・ボス・野生種）をB30-B20実データ+B19-B1ゾーンデータで構造化
4. **Phase 3**: 拠点+セーブ（`AdventureMode.js`）
   - Firestore `users/{uid}/adventure/save`（`party`/`box`/`money`/`clearedFloors`/`currentFloor`/`seenIds`）
   - パーティ編成・技装備変更・ショップUI（4.17）
5. **Phase 4**: 探索フェーズ（ノード選択UI）
   - 4択＋ヒント表示、罠システム、探索テキスト演出（4.12）
6. **Phase 5**: アドベンチャー戦闘（`AdventureBattle.js`）
   - レベル補正ダメージ計算、`getWildMoveSet`/`getEliteMoves`/アーキタイプチーム
   - 捕獲UI（`AdventureCapture.js`と接続）
7. **Phase 6**: B30〜B20 実装+プレイテスト（初回リリーススコープ）✅
8. **Phase 7**: 技演出（CSS/SVGアニメーション）✅
9. **Phase 8**: B19〜B1 実装+ラスボス(ヴァーサス)戦+エピローグ ✅
10. **Phase 9**（未着手）: クリア後やり込みコンテンツ本体（4.13）
